public interface Paymentways {
    abstract void gpay();
    abstract void paytm();
    abstract void phonepe();
    /*
    In Java, interface members (methods and variables) have default modifiers.
    By default, interface methods are abstract 
    and public, and interface variables are public, static, and final. 
                                */
}
